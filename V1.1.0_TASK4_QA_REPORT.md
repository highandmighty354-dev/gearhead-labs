# V1.1.0 Gearhead Labs — Task 4 QA Report

## Inventory
- Production registry: **575 total entries including Dashboard**.
- Base registry: **275**.
- Diesel expansion: **26**.
- E1 engineering expansion: **178**.
- E1.0.1 conversion/shop-math expansion: **96**.
- IDs are disjoint and unique across the four sets.
- Production lab distribution: **44 gasoline / 42 diesel / 24 EV / 25 towing / 439 universal** (Dashboard excluded from lab counts).

## Render coverage
- Base registry: **275/275** renderer keys present.
- Base renderer execution harness: **275/275 PASS** under deterministic default-input stubs after Task 4 repairs.
- Diesel renderer execution: **26/26 PASS**.
- E1/E1.0.1 formula implementations: **279/279 PASS** in a separate Python reference-evaluation pass against the JavaScript evaluation path.

## Defects found and repaired during Task 4
1. `curtain_area`: local variable `vd` shadowed the `vd()` input reader, causing a JavaScript temporal-dead-zone failure. Corrected to `valve_dia`.
2. `drill_decimal`: `sv('drill_letter')` could be null before a selection existed, causing `.trim()` to throw. Corrected with safe normalization/fallback.
3. Added a render-boundary safety guard so `NaN`, `Infinity`, `-Infinity`, and `undefined` cannot be presented as calculator results; invalid domains now return an explicit Invalid Input state.
4. Existing Task 4 mathematical corrections retained: BMEP, natural/ride frequency, lateral acceleration, Ackermann geometry, dynamic pressure, engine air density, dynamic compression geometry, N·m → lb-in labeling/conversion, CG-height tilt-test equation, and caliper clamp-force semantics.

## Regression evidence
- Existing R2.8 airflow/valvetrain regression evidence records **300/300 structural passes**, **25/25 new render-function passes**, and representative formula/unit tests passing. The historical report explicitly states that a full live-browser click-through was not claimed.
- Existing 275-calculator validation planning assigns sensitivity, persistence, units, boundary, and known-answer tests across the catalog, but older records show that many independent known-answer tests remained pending.

## Current gate
**STATIC / NODE QA: PASS.**

The source-level inventory, renderer coverage, deterministic execution, formula cross-runtime checks, and Task 4 defect repairs are complete.

**LIVE BROWSER CERTIFICATION: NOT CLAIMED.** The available execution environment did not provide a usable Chromium runtime for a full 575-calculator click-through. No false browser-certification claim is made.

## Release rule
Task 4 should only be called fully production-certified after a real browser session verifies the published production build and the remaining independent known-answer matrix is exercised against the live calculator UI.
